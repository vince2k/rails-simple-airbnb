<nav class="navbar navbar-expand-lg fixed-top">
  <div class="container-fluid">
    <%= link_to "Simple Airbnb", flats_path, class: "text-light" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="ms-auto mt-2">
        <%= form_tag flats_path, method: 'get', class: "d-flex align-items-center gap-2" do %>
          <%= text_field_tag :search, params[:search], class: "form-control me-2", placeholder: "Search" %>
            <div class="d-flex align-items-center gap-2" style="white-space: nowrap;">
              <%= submit_tag "Search", class: "btn btn-outline-success" %>
              <%= link_to "New flat", new_flat_path, class: "btn btn-outline-primary" %>
            </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>
